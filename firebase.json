{
  "//": "Emulator-only configuration. Firebase Hosting/Firestore are no longer deployed; this exists solely so the Firestore data migration can be tested against the local emulator.",
  "firestore": {
    "rules": "firestore.rules"
  },
  "emulators": {
    "firestore": {
      "host": "127.0.0.1",
      "port": 8080
    },
    "singleProjectMode": true
  }
}
