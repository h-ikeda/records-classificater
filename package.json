{
  "devDependencies": {
    "@parcel/transformer-vue": "^2.8.2",
    "firebase": "^9.15.0",
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "vue": "^3.2.45"
  },
  "scripts": {
    "start": "firebase emulators:exec --only=auth,firestore 'parcel src/index.html'",
    "build": "parcel build src/index.html",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:rules:prod": "firebase --project records-classificater deploy --only firestore:rules"
  }
}
